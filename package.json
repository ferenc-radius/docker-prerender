{
  "name": "docker-prerender",
  "version": "1.0.0",
  "description": "Dockerfile to build the prerender container image",
  "main": "server.js",
  "dependencies": {
    "prerender": "^3.22.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "start": "node server.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ferenc-radius/docker-prerender.git"
  }
}
